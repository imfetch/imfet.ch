<%

@body_id = 'install'

%><%=

content_tag(:div,
  content_tag(:div, :id => :teaser) {
    tag(:img, :src => assets_path('title.png')) +
    content_tag(:p, 'Coming soon', :class => :t)
  },
  :id => 'wrapper'
)

%>
